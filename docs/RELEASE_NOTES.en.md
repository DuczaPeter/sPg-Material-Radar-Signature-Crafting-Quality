# Release Notes — 2026-09-22 professional GitHub package

This package does not create a new application version. It preserves the current production HTML byte-for-byte and adds a professional GitHub documentation/validation layer around it.

## Added

- HU/EN README and user guide;
- architecture, data-source, testing, release, limitation and roadmap docs;
- AGENTS/STATUS/VERSION metadata;
- licence/notice/third-party/privacy/security docs;
- GitHub issue/PR templates and Actions static gate;
- manifest/hash system;
- browser fallback runtime evidence.

## Validation

- canonical app source-verified against GitHub main;
- static gate PASS;
- fallback browser interaction PASS on desktop + mobile;
- external LIVE browser fetch UNVERIFIED in this run.

## User-facing application

No production business logic or UI behaviour was intentionally changed by this packaging step.
