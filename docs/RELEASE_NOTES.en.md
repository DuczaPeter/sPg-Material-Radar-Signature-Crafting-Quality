# Release notes — 2026-09-23

## r3 — V4.2 (`2026.09.23-r3-v4.2-standard`)

The production HTML is unchanged in this package as well. The package restores the dot-prefixed files missing from the published repository (`.github/`, `.gitignore`), adds a `.gitattributes` line-ending policy, replaces the project-local standard with the canonical V4.2 and corrects the `VISUAL VERIFIED` label to the standard `RUNTIME VERIFIED`.

## r2 — V4.1 (HISTORICAL)

This release **does not modify production HTML behaviour**. Its purpose is to migrate the repository/release layer to the V4.1 professional GitHub release standard.

New release-layer work includes the project-local release standard, Release Contract, stronger gate/secret checks, real browser screenshots, technical diagrams and V4.1 validation evidence.

The production HTML SHA-256 remains unchanged: `28be33eae5f2b53f375e52003a7af282ff8b97243fdb14ee0b28b531be9402f4`.

Release status: **READY WITH LIMITATIONS**. LIVE third-party browser E2E and GitHub Pages pixel parity are not proven in this packaging run.
