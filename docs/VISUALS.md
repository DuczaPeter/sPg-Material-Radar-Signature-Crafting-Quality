# Visual documentation

## Real UI screenshots

- `assets/ui-desktop.png` — real Chromium render of the packaged HTML at 1440×1000 under the offline/fallback runtime test condition.
- `assets/ui-mobile.png` — real Chromium render at 390×844 under the same condition.

These screenshots are runtime evidence only for the UI state and interactions exercised by the fallback harness. They are **not** evidence that LIVE third-party network fetches worked.

## Generated project graphics

- `assets/data-flow.svg` — project-specific technical data-flow diagram created from the documented application architecture.
- `assets/social-preview.svg` and `.png` — project-specific original social-preview artwork. It contains no third-party game screenshot, logo or marketing asset.

Generated diagrams/preview graphics are documentation assets, not runtime screenshots or runtime evidence.

The repository asset does not automatically configure GitHub's repository social preview. That setting must be applied separately in GitHub UI/API if desired.
